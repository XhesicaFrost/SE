server.port=12345

# 数据库配置
spring.datasource.url=jdbc:mysql://localhost:3306/takeout?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.username=root
spring.datasource.password=messi030712
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA配置
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# MyBatis配置
mybatis.mapper-locations=classpath:mapper/*.xml
mybatis.type-aliases-package=com.blm.takeout.entity
mybatis.configuration.map-underscore-to-camel-case=true

# 日志配置
logging.level.com.blm.takeout=debug
logging.level.org.springframework.web=debug
logging.level.org.mybatis=debug

# 文件上传配置
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
server.tomcat.max-http-form-post-size=52428800
server.tomcat.connection-timeout=30s
server.tomcat.max-connections=200
server.tomcat.max-threads=100

# JWT配置
app.jwtSecret=Juggr%ZkqkGkUiPPzBL!VC7+UZo0tMS*
app.jwtExpirationMs=86400000

# 文件上传目录
app.upload.dir=uploads

# 微信支付配置（模拟环境）
wechat.pay.appId=wx_mock_app_id
wechat.pay.mchId=mock_mch_id
wechat.pay.apiKey=mock_api_key
wechat.pay.notifyUrl=http://localhost:12345/api/pay/notify
